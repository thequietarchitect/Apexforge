P11 Capability Pulse — Visual Studio Layout

Each .apex source contains one ordinary top-level declaration so the frozen
Visual Studio/LSP one-node parser can analyze it without false EOF diagnostics.

Open src/main.apex to edit the primary directive.
Use apexforge.json to run the complete eight-source project.
